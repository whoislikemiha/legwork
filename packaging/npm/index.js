#!/usr/bin/env node
// Placeholder: legwork is a Go binary distributed via GitHub releases.
// This npm package reserves the name and will later ship a binary-fetching wrapper.
console.error(
  "legwork is not distributed via npm yet.\n" +
    "Install: https://github.com/whoislikemiha/legwork (see Releases)\n"
);
process.exit(1);
